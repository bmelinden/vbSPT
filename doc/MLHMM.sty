% Here, I like to collect various useful shorthands. Many journals do
% not support user sty files, and hence the below lines must be pasted
% into the preamble of the main paper before submission.

%shorthand notation
\newcommand{\kBT}{k_\text{B}T} % kBT
\newcommand{\mean}[1]{\left\langle #1\right\rangle} % average
\newcommand{\matris}[1]{\mathbf{#1}}
%\renewcommand{\vec}[1]{\vec{#1}}
\newcommand{\Eq}[1]{Eq.~\eqref{#1}}
\newcommand{\Fig}[1]{Fig.~\ref{#1}}
\newcommand{\x}{\vec{x}}
\newcommand{\Dir}{\text{Dir}} % Dirichlet pdf
\newcommand{\Var}{\text{Var}} % variance
\newcommand{\std}{\text{std}} % standard deviation


